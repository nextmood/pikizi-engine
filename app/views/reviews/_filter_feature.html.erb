<% if feature.is_compatible_grammar(:comparator) %>
    <%= feature.tags.collect { |t| "#{check_box_tag(:toto)} #{t.label}" }.join(', ') %>
<% else %>
    unknown filter <%= "#{feature.label} class=#{feature.class}" %>
<% end %>


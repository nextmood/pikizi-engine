    <td><%= object_state(opinion) %></td>
    <td>
        <%= link_to_remote(opinion.to_html, :url => { :controller => "opinions", :action => "edit_opinion_from_collection", :id => opinion.id }, :html => {:class => "pkz_link", :title => ("original import=" << opinion.original_import.inspect.gsub('"', '')) }) %>
    </td>
    <td><%= opinion.original_import[:op_score] %></td>
    <td><%= opinion.op_conf %></td>
    <td><%= opinion.original_import[:score] %></td>
    <td align="right">
        <% if opinion.censor_date %>
          <span class='pkz_small'>
            <%= "<span class='pkz_small'>#{User.find(opinion.censor_author_id).rpx_username} on #{opinion.censor_date}: #{opinion.censor_comment}" %>
            <%= "<br/>comparator was originally <b>#{opinion.original_import[:operator]}</b>" unless opinion.original_import[:operator] == opinion.operator_type %>
          </span>
        <% end %>

    </td>
